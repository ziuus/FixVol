import SwiftUI
import shared

struct ContentView: View {
    var body: some View {
        VStack {
            Text("FixVol KMP")
                .font(.largeTitle)
            Text("Cross-platform domain logic setup complete.")
        }
    }
}

struct ContentView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
